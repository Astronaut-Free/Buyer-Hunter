# Design QA — 买家猎手 Demo

> **已归档** — demo React 前端已退役（代码保留于 `demo/`，仅作视觉参考）；当前产品前端为 `site/`（门户，4180）+ `agent/`（Agent 运行时前端，3317）。本报告为退役前 QA 记录。

## 验收对象

- 视觉参考：`output/audit/01-website-wireframe.png`
- 同视口对比：`output/qa/reference-vs-demo.png`
- 桌面实现：`output/qa/demo-desktop.png`（1440 × 1100）
- 小屏实现：`output/qa/demo-mobile-500.png`（500 × 844）
- 主链：今日机会 → 机会详情 → 会员模拟 → 解锁 → 联系入口 → 跟进状态

## 对比结论

| 维度 | 结果 | 依据 |
|---|---|---|
| 视觉语言 | 通过 | 延续参考稿白/金/蓝、细边框、浅底、低阴影和系统中文字体 |
| 信息层级 | 通过 | 首页优先展示目标产品、36→12→5 漏斗和 Top 5，不新增营销 Hero |
| 组件一致性 | 通过 | 卡片圆角、标签、边框、数据卡和列表密度与线框同构 |
| 商业模式 | 通过 | 去除公开产品库、买家发布台和成交工作台；加入免费预览与会员解锁 |
| 响应式 | 通过 | 500px 小屏无横向裁切；侧栏改为 4 项底部导航，会员状态保留在页头 |
| 可访问性 | 通过 | 图标按钮有 aria-label；主弹层有 dialog/aria-modal；按钮和状态不只依赖颜色 |
| 离线性 | 通过 | 页面无外部字体和图片依赖，核心数据使用本地缓存模拟数据 |

## 已修复问题

| 等级 | 问题 | 修复 |
|---|---|---|
| P0 | Phosphor 包不导出 `Radar`，生产构建失败 | 改用已存在的 `GlobeHemisphereWest`；构建通过 |
| P1 | 手机页头继承桌面网格，账户/会员按钮可能溢出 | 小屏改为两列页头，隐藏非核心账户按钮并保留会员入口 |
| P1 | 参考稿“产品库”会误导为卖家展示商品 | 改为私有“我的匹配条件”，明确不会公开 |
| P1 | 联系方式在免费态可能被前端遮罩式泄露 | Demo 状态中仅解锁后渲染完整字段；正式 API 合同要求服务端裁剪 |
| P2 | 跟进阶段按钮只有视觉无状态 | 加入 FOLLOW_UP / NEGOTIATING / WON / LOST 可切换状态 |
| P2 | 默认页面标题为 Prototype | 改为产品标题与中文语言声明 |

## 构建与结构验证

- `npm run build`：通过，Vite 生产构建及 Sites 包装完成。
- `npm run test:sites`：4/4 通过。
- `db/schema.sql`：SQLite 内存执行通过。
- `contracts/openapi.yaml`：YAML 解析通过，共 13 个 path。

## 已知 Demo 边界

- 买家公司、邮箱、采购 URL 均为模拟数据；不代表真实联系方式。
- 会员为本地状态模拟，不接真实支付。
- 抓取、清洗、实体去重与 Agent 评分以合同、Schema 和缓存数据表达，未在浏览器内伪造全网实时抓取。
- 筛选按钮和保存匹配条件属于次级控件，本轮为视觉态；核心会员解锁链路为可交互状态。

final result: passed
