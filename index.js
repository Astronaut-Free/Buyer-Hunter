import { createServer } from 'node:http';
import { readFile, writeFile, mkdir } from 'node:fs/promises';
import { existsSync } from 'node:fs';
import { extname, join, normalize } from 'node:path';
import { fileURLToPath } from 'node:url';
import { randomBytes, scryptSync, timingSafeEqual, createHmac } from 'node:crypto';
import { decideHandoff, calculateMatch, evaluateMarketAccess } from './decision-engine.js';
import { createCapabilityAdapter, withRetry } from './capability-adapter.js';
import { guardBuyerOutput } from './output-guard.js';
import { loadFreeOpportunities } from './repository.js';

const ROOT = fileURLToPath(new URL('..', import.meta.url));
const SERVER_DIR = fileURLToPath(new URL('.', import.meta.url));
const STATE_FILE = join(SERVER_DIR, 'agent-state.json');
const PORT = Number(process.env.PORT || 3317);
const DEEPSEEK_BASE_URL = process.env.DEEPSEEK_BASE_URL || 'https://api.deepseek.com';
const DEEPSEEK_MODEL = process.env.DEEPSEEK_MODEL || 'deepseek-chat';
const AGENT_VERSION = 'qianpulse-agent-0.2.0';
const SESSION_DAYS = 7;
const CAPABILITIES = [
  { capability_id: 'demand.normalize', version: '1.0.0', description: '标准化买家采购需求' },
  { capability_id: 'buyer.intent', version: '1.0.0', description: '读取买家采购意向结果' },
  { capability_id: 'supply.match', version: '1.0.0', description: '读取卖家能力匹配结果' },
  { capability_id: 'market.access', version: '1.0.0', description: '核验目标市场准入资料' },
  { capability_id: 'conversation.qualify', version: '1.0.0', description: '计算沟通字段完整度' },
  { capability_id: 'reply.draft', version: '1.0.0', description: '生成待人工确认的回复草稿' }
];
const ROUTING_POLICY = { budget: ['buyer.intent', 'conversation.qualify'], price: ['buyer.intent', 'conversation.qualify', 'reply.draft'], delivery_date: ['supply.match', 'conversation.qualify', 'reply.draft'], quantity: ['supply.match', 'buyer.intent', 'conversation.qualify'], certification: ['market.access', 'supply.match', 'reply.draft'], specification: ['supply.match', 'market.access'], seller_capacity: ['supply.match'], seller_profile: ['supply.match'] };

const now = () => new Date().toISOString();
const id = prefix => `${prefix}_${Date.now()}_${randomBytes(4).toString('hex')}`;
const hash = value => createHmac('sha256', 'qianpulse-integrity').update(JSON.stringify(value)).digest('hex');
const sendJson = (res, status, body) => { res.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8', 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Headers': 'Content-Type, Authorization', 'Access-Control-Allow-Methods': 'GET, POST, OPTIONS' }); res.end(JSON.stringify(body)); };
async function readBody(req) { if (req._payload) return req._payload; let raw = ''; for await (const chunk of req) raw += chunk; if (!raw) return {}; try { req._payload = JSON.parse(raw); return req._payload; } catch { throw new Error('请求体必须是 JSON'); } }
const initialState = () => ({ users: {}, sessions: {}, events: {}, opportunities: { opp_demo_001: { id: 'opp_demo_001', buyer: { id: 'buyer_demo_001', name: '美国 · 饮料品牌', market: 'US' }, seller: { id: 'seller_demo_001', name: '贵州示例茶企' }, fields: { product: '有机饮品级贵州抹茶', quantity: '500 kg/月以上', certification: 'USDA Organic', sample_required: true, oem_required: true }, fit_score: 72, intent_score: 81, conversation_score: 58, decision: 'AI_NURTURING', status: '待确认', evidence_ids: ['ev_demo_001'], updated_at: now() } }, threads: {}, messages: {}, runs: {}, steps: {}, checkpoints: {}, approvals: {}, idempotency: {}, traces: [] });
let state; let persistTimer;
async function persist() { await mkdir(SERVER_DIR, { recursive: true }); await writeFile(STATE_FILE, JSON.stringify(state, null, 2)); }
function persistSoon() { clearTimeout(persistTimer); persistTimer = setTimeout(() => persist().catch(console.error), 20); }
async function loadState() { if (!existsSync(STATE_FILE)) { state = initialState(); await persist(); return; } try { state = JSON.parse(await readFile(STATE_FILE, 'utf8')); state.events ||= {}; state.users ||= {}; state.sessions ||= {}; } catch { state = initialState(); await persist(); } }

function passwordHash(password, salt = randomBytes(16).toString('hex')) { return `${salt}:${scryptSync(String(password), salt, 64).toString('hex')}`; }
function verifyPassword(password, stored) { try { const [salt, digest] = stored.split(':'); return timingSafeEqual(scryptSync(String(password), salt, 64), Buffer.from(digest, 'hex')); } catch { return false; } }
function tokenFor(user) { const payload = Buffer.from(JSON.stringify({ sub: user.id, role: user.role, exp: Date.now() + SESSION_DAYS * 86400000 })).toString('base64url'); const signature = createHmac('sha256', process.env.AUTH_SECRET || 'change-me-in-production').update(payload).digest('base64url'); return `${payload}.${signature}`; }
function userFromRequest(req) { const token = String(req.headers.authorization || '').replace(/^Bearer\s+/i, ''); const session = state.sessions[token]; if (!session || session.expires_at < Date.now()) return null; return state.users[session.user_id] || null; }
const publicUser = user => ({ id: user.id, email: user.email, role: user.role, profile: user.profile, created_at: user.created_at });

function resolveOpportunity(payload) { if (payload.opportunity_id && state.opportunities[payload.opportunity_id]) return state.opportunities[payload.opportunity_id]; if (payload.thread_id && state.threads[payload.thread_id]) return state.opportunities[state.threads[payload.thread_id].opportunity_id]; return null; }
function canAccess(user, opportunity) { if (!user || !opportunity) return false; if (user.role === 'INTERNAL') return true; if (opportunity.id === 'opp_demo_001') return user.role === 'SELLER' || user.role === 'BUYER'; if (user.role === 'BUYER') return opportunity.buyer?.id === user.id; if (user.role === 'SELLER') return opportunity.seller?.id === user.id; return false; }
function projectOpportunity(opportunity, role) { if (role === 'BUYER') { const safe = guardBuyerOutput(JSON.stringify({ product: opportunity.fields.product, quantity: opportunity.fields.quantity }), { approved: true }); return { id: opportunity.id, buyer: opportunity.buyer, fields: safe.allowed ? { product: opportunity.fields.product, quantity: opportunity.fields.quantity, certification: opportunity.fields.certification } : {}, status: opportunity.status, decision: opportunity.decision }; } if (role === 'SELLER') return { ...opportunity, internal_notes: undefined, risk_topics: undefined }; return opportunity; }
function changedFieldsFromMessage(content) { const text = String(content || '').toLowerCase(); const fields = []; if (/预算|budget|价格|price|报价/.test(text)) fields.push('budget'); if (/交期|到货|delivery|date|q[1-4]|月份|deadline/.test(text)) fields.push('delivery_date'); if (/数量|吨|kg|公斤|采购量|quantity|volume/.test(text)) fields.push('quantity'); if (/认证|有机|organic|fda|jas|证书|certificate/.test(text)) fields.push('certification'); if (/规格|目数|包装|spec|mesh|package/.test(text)) fields.push('specification'); return [...new Set(fields)]; }
async function askDeepSeek(message, opportunity) {
  if (!process.env.DEEPSEEK_API_KEY) return { answer: null, provider: 'rules-fallback', reason: '未配置 DEEPSEEK_API_KEY' };
  const controller = new AbortController(); const timer = setTimeout(() => controller.abort(), 18000);
  const context = { buyer: opportunity.buyer, demand: opportunity.fields, scores: { fit: opportunity.fit_score, intent: opportunity.intent_score }, evidence: opportunity.evidence || opportunity.evidence_ids || [], risk: opportunity.risk_topics || [] };
  const intakeMode=String(message).includes('卖家当前供给档案'); const system = intakeMode ? '你是黔脉 QianPulse 的卖家入驻顾问。通过自然对话收集卖家的产品、规格、MOQ、月产能、目标市场、目标买家类型和已有认证。严格读取提供的供给档案和历史对话，不要重复询问已经明确回答过的字段。一个回答只能填入一个字段；如果用户只说数字、单位或“都有”等模糊内容，必须先追问确认其对应字段，禁止猜测或复制到多个字段。每次只问一个最关键的缺口，直接用自然中文提问，不要输出 JSON、字段名、技术状态或固定模板。' : '你是黔脉 QianPulse 的 B2B 出口商机顾问。只基于提供的买家上下文回答，不得编造未提供的数量、法规或认证结论。回答用简洁中文，结构为：结论、依据、不确定性、建议下一步。涉及法律、出口合规、食品安全时必须明确“需要人工/专业机构复核”，不能给出保证。';
  try {
    const response = await fetch(`${DEEPSEEK_BASE_URL.replace(/\/$/, '')}/chat/completions`, { method: 'POST', signal: controller.signal, headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${process.env.DEEPSEEK_API_KEY}` }, body: JSON.stringify({ model: DEEPSEEK_MODEL, temperature: 0.2, messages: [{ role: 'system', content: system }, { role: 'user', content: `买家上下文：${JSON.stringify(context)}\n卖家问题：${message}` }] }) });
    if (!response.ok) throw new Error(`DeepSeek HTTP ${response.status}`);
    const data = await response.json(); const answer = data.choices?.[0]?.message?.content?.trim(); if (!answer) throw new Error('DeepSeek 返回为空');
    return { answer, provider: 'deepseek', model: DEEPSEEK_MODEL };
  } catch (error) { return { answer: null, provider: 'rules-fallback', reason: error.name === 'AbortError' ? 'DeepSeek 请求超时' : error.message }; } finally { clearTimeout(timer); }
}
function isAcknowledgement(content) { return /^(thanks|thank you|谢谢|收到|好的|ok|okay|got it)[.!！。 ]*$/i.test(String(content || '').trim()); }
function handoffDecision(o) { return decideHandoff(o); }
function getThread(opportunityId, party) { const found = Object.values(state.threads).find(t => t.opportunity_id === opportunityId && t.party === party); if (found) return found; const thread = { thread_id: id('thread'), opportunity_id: opportunityId, party, channel: 'simulated', status: 'IDLE', created_at: now(), last_message_at: null }; state.threads[thread.thread_id] = thread; return thread; }

async function invokeCapability(capabilityId, opportunity, run, event) { const capability = CAPABILITIES.find(item => item.capability_id === capabilityId); if (!capability) return null; const changed = event.changed_fields || []; let result = { capability_id: capabilityId, capability_version: capability.version, run_status: 'DONE', changed_fields: changed, missing_evidence: [], evidence_refs: [], human_review_required: false, domain_result: {} }; if (capabilityId === 'buyer.intent') result.domain_result.intent_score = opportunity.intent_score; if (capabilityId === 'supply.match') result.domain_result.fit_score = opportunity.fit_score; if (capabilityId === 'conversation.qualify') result.domain_result.conversation_score = opportunity.conversation_score; if (capabilityId === 'market.access') { result.domain_result.access_status = opportunity.fields.certification ? 'REVIEW' : 'MORE_EVIDENCE'; if (!opportunity.fields.certification) { result.run_status = 'MORE_EVIDENCE'; result.missing_evidence = ['certification']; } } if (capabilityId === 'reply.draft') { result.human_review_required = true; result.domain_result.draft = { status: 'DRAFT_READY', text: '您好，我们已收到贵方更新。以下内容为待人工确认草稿。' }; } const step = { step_id: id('step'), run_id: run.run_id, sequence: Object.values(state.steps).filter(item => item.run_id === run.run_id).length + 1, step_type: 'CAPABILITY', capability_id: capabilityId, capability_version: capability.version, input_hash: hash({ opportunity_id: opportunity.id, event_id: event.event_id }), output_hash: hash(result), status: result.run_status, started_at: now(), completed_at: now(), evidence_refs: result.evidence_refs, result }; state.steps[step.step_id] = step; return result; }
const legacyInvokeCapability = invokeCapability;
invokeCapability = async function(capabilityId, opportunity, run, event) {
  const result = await withRetry(() => legacyInvokeCapability(capabilityId, opportunity, run, event), { retries: 2, onRetry: (attempt, error) => state.traces.push({ trace_id: id('trace'), run_id: run.run_id, span_type: 'RetrySpan', payload: { capability_id: capabilityId, attempt, error: error.message }, timestamp: now() }) });
  if (capabilityId === 'supply.match') result.domain_result.real_matches = calculateMatch(opportunity, state.products || []);
  if (capabilityId === 'market.access') result.domain_result.access_evaluation = evaluateMarketAccess(opportunity, opportunity.seller_profile || {});
  const step = Object.values(state.steps).reverse().find(item => item.run_id === run.run_id && item.capability_id === capabilityId); if (step) { step.result = result; step.output_hash = hash(result); }
  return result;
};

async function createRun(payload, user) { const idem = String(payload.idempotency_key || '').trim(); if (!idem) return { status: 400, body: { code: 'IDEMPOTENCY_KEY_REQUIRED', error: '必须提供 idempotency_key' } }; if (state.idempotency[idem]) return { status: 200, body: state.idempotency[idem] }; const opportunity = resolveOpportunity(payload); if (!opportunity) return { status: 422, body: { code: 'NEEDS_CONTEXT', message: '无法可靠绑定 Opportunity，禁止猜测。' } }; if (!canAccess(user, opportunity, payload.event_type === 'BUYER_MESSAGE' ? 'create_message' : 'read')) return { status: 403, body: { error: '无权访问这笔 Opportunity' } }; const role = user.role; const thread = payload.thread_id ? state.threads[payload.thread_id] : getThread(opportunity.id, role === 'BUYER' ? 'BUYER' : 'SELLER'); const event = { event_id: id('evt'), event_type: payload.event_type || (role === 'BUYER' ? 'BUYER_MESSAGE' : 'SELLER_QUERY'), actor_role: role, actor_id: user.id, opportunity_id: opportunity.id, thread_id: thread.thread_id, payload, source: payload.source || 'api', timestamp: now(), idempotency_key: idem, created_at: now() }; state.events[event.event_id] = event; const run = { run_id: id('run'), opportunity_id: opportunity.id, trigger_event_id: event.event_id, status: 'RUNNING', started_at: now(), completed_at: null, state_before: opportunity.status, state_after: null, capabilities_called: [], decision_before: opportunity.decision, decision_after: null, agent_version: AGENT_VERSION }; state.runs[run.run_id] = run; state.traces.push({ trace_id: id('trace'), run_id: run.run_id, span_type: 'EventRouteSpan', payload: { event_type: event.event_type, actor_role: role }, timestamp: now() }); const content = payload.message || payload.content || ''; let changed = payload.changed_fields || (event.event_type === 'BUYER_MESSAGE' ? changedFieldsFromMessage(content) : []); if (event.event_type === 'BUYER_MESSAGE' && isAcknowledgement(content)) changed = []; const capabilities = event.event_type === 'BUYER_MESSAGE' && !changed.length ? [] : (payload.capabilities || [...new Set(changed.flatMap(field => ROUTING_POLICY[field] || []))]); if (!capabilities.length && role === 'SELLER') capabilities.push('buyer.intent', 'supply.match', 'conversation.qualify'); event.changed_fields = changed; for (const capabilityId of capabilities) { run.capabilities_called.push(capabilityId); const result = await invokeCapability(capabilityId, opportunity, run, event); if (result?.run_status === 'MORE_EVIDENCE') run.status = 'WAITING_EVIDENCE'; } const decision = handoffDecision(opportunity); opportunity.decision = decision.decision; opportunity.updated_at = now(); run.decision_after = decision.decision; run.state_after = opportunity.status; if (run.status === 'RUNNING') run.status = 'COMPLETED'; if (event.event_type === 'BUYER_MESSAGE') { thread.last_message_at = now(); thread.status = changed.length ? 'NEEDS_ANALYSIS' : 'REPLIED'; state.messages[event.event_id] = { ...event, direction: 'INBOUND', content }; } let approval = null; if (run.capabilities_called.includes('reply.draft')) { approval = { approval_id: id('approval'), opportunity_id: opportunity.id, run_id: run.run_id, action_type: 'BUYER_MESSAGE_DRAFT', payload: { content: '待人工确认：请根据已验证信息编辑回复。' }, risk_summary: '涉及对外商业信息，必须人工确认。', status: 'PENDING', requested_by: user.id, approved_by: null, created_at: now(), approved_at: null }; state.approvals[approval.approval_id] = approval; run.status = 'WAITING_APPROVAL'; } const checkpoint = { checkpoint_id: id('cp'), run_id: run.run_id, opportunity_id: opportunity.id, step: run.capabilities_called.length, state: run.status, input_hash: hash(payload), output_hash: hash({ decision, opportunity }), created_at: now() }; state.checkpoints[checkpoint.checkpoint_id] = checkpoint; run.completed_at = now(); const response = { run, opportunity: projectOpportunity(opportunity, role), decision, changed_fields: changed, capabilities_called: run.capabilities_called, checkpoint_id: checkpoint.checkpoint_id, approval }; state.idempotency[idem] = response; persistSoon(); return { status: 201, body: response }; }

async function authHandler(req, res, path) { const payload = await readBody(req); if (req.method === 'POST' && path === '/api/v1/auth/register') { const role = ['SELLER', 'BUYER'].includes(payload.role) ? payload.role : null; const email = String(payload.email || '').trim().toLowerCase(); const password = String(payload.password || ''); if (!role) return sendJson(res, 400, { error: 'role 必须是 SELLER 或 BUYER' }); if (!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email)) return sendJson(res, 400, { error: '请输入有效邮箱' }); if (password.length < 8) return sendJson(res, 400, { error: '密码至少 8 位' }); if (Object.values(state.users).some(user => user.email === email)) return sendJson(res, 409, { error: '该邮箱已注册' }); const user = { id: id(role.toLowerCase()), email, role, password_hash: passwordHash(password), profile: { company_name: String(payload.company_name || ''), country: String(payload.country || ''), contact_name: '', verification_status: 'PENDING', profile_completion: 0 }, created_at: now(), last_login_at: null }; state.users[user.id] = user; const token = tokenFor(user); state.sessions[token] = { user_id: user.id, expires_at: Date.now() + SESSION_DAYS * 86400000, created_at: now() }; persistSoon(); return sendJson(res, 201, { user: publicUser(user), token, next: role === 'SELLER' ? 'seller-onboarding' : 'buyer-onboarding' }); } if (req.method === 'POST' && path === '/api/v1/auth/login') { const email = String(payload.email || '').trim().toLowerCase(); const user = Object.values(state.users).find(item => item.email === email); if (!user || !verifyPassword(payload.password, user.password_hash)) return sendJson(res, 401, { error: '邮箱或密码错误' }); const token = tokenFor(user); state.sessions[token] = { user_id: user.id, expires_at: Date.now() + SESSION_DAYS * 86400000, created_at: now() }; user.last_login_at = now(); persistSoon(); return sendJson(res, 200, { user: publicUser(user), token, next: user.role === 'SELLER' ? 'seller-onboarding' : 'buyer-onboarding' }); } if (req.method === 'GET' && path === '/api/v1/auth/me') { const user = userFromRequest(req); return user ? sendJson(res, 200, { user: publicUser(user) }) : sendJson(res, 401, { error: '未登录' }); } if (req.method === 'POST' && path === '/api/v1/auth/logout') { const token = String(req.headers.authorization || '').replace(/^Bearer\s+/i, ''); delete state.sessions[token]; persistSoon(); return sendJson(res, 200, { ok: true }); } return sendJson(res, 404, { error: '认证接口不存在' }); }

async function handleV1(req, res, path) { const payload = await readBody(req); const user = userFromRequest(req); if (req.method === 'GET' && path === '/api/v1/agent/capabilities') return sendJson(res, 200, CAPABILITIES); const runMatch = path.match(/^\/api\/v1\/agent\/runs\/([^/]+)$/); if (req.method === 'GET' && runMatch) { const run = state.runs[runMatch[1]]; const opportunity = run && state.opportunities[run.opportunity_id]; if (!run || !canAccess(user, opportunity)) return sendJson(res, 404, { error: 'Run 不存在' }); return sendJson(res, 200, user.role === 'INTERNAL' ? run : { ...run, internal_debug: undefined }); } if (req.method === 'POST' && path === '/api/v1/agent/runs') { if (!user) return sendJson(res, 401, { error: '请先登录后使用 Agent' }); return respond(res, await createRun(payload, user)); } const msgMatch = path.match(/^\/api\/v1\/opportunities\/([^/]+)\/messages$/); if (req.method === 'POST' && msgMatch) { if (!user) return sendJson(res, 401, { error: '请先登录后使用 Agent' }); return respond(res, await createRun({ ...payload, opportunity_id: msgMatch[1], event_type: 'BUYER_MESSAGE' }, user)); } const threadsMatch = path.match(/^\/api\/v1\/opportunities\/([^/]+)\/threads$/); if (req.method === 'GET' && threadsMatch) { const opportunity = state.opportunities[threadsMatch[1]]; if (!canAccess(user, opportunity)) return sendJson(res, 403, { error: '无权查看对话' }); return sendJson(res, 200, Object.values(state.threads).filter(thread => thread.opportunity_id === threadsMatch[1]).map(thread => user.role === 'INTERNAL' || thread.party === (user.role === 'BUYER' ? 'BUYER' : 'SELLER') ? thread : { thread_id: thread.thread_id, party: thread.party, status: thread.status })); } const approvalMatch = path.match(/^\/api\/v1\/approvals\/([^/]+)$/); if (req.method === 'POST' && approvalMatch) { if (!user || user.role !== 'INTERNAL') return sendJson(res, 403, { error: '只有 INTERNAL 可以审批外部动作' }); const approval = state.approvals[approvalMatch[1]]; if (!approval) return sendJson(res, 404, { error: 'Approval 不存在' }); if (!['APPROVED', 'EDITED', 'REJECTED'].includes(payload.status)) return sendJson(res, 400, { error: 'status 必须是 APPROVED、EDITED 或 REJECTED' }); approval.status = payload.status; approval.approved_by = user.id; approval.approved_at = now(); approval.payload_hash = hash(payload.edited_payload || approval.payload); persistSoon(); return sendJson(res, 200, approval); } const traceMatch = path.match(/^\/api\/v1\/agent\/runs\/([^/]+)\/trace$/); if (req.method === 'GET' && traceMatch) { const run = state.runs[traceMatch[1]]; const opportunity = run && state.opportunities[run.opportunity_id]; if (!user || user.role !== 'INTERNAL' || !run || !canAccess(user, opportunity)) return sendJson(res, 403, { error: '只有 INTERNAL 可以查看完整 Trace' }); return sendJson(res, 200, state.traces.filter(trace => trace.run_id === run.run_id)); } return sendJson(res, 404, { error: '未找到 Agent 控制面接口' }); }
function respond(res, result) { return sendJson(res, result.status, result.body); }
const controlHandler = handleV1;
handleV1 = async function(req, res, path) {
  if (req.method === 'POST' && path === '/api/v1/agent/intake') {
    const user = userFromRequest(req); if (!user) return sendJson(res, 401, { error: '请先登录后使用 AI 对话' });
    const body = await readBody(req); const sellerContext = { buyer: { role: 'SELLER' }, fields: body.profile || {}, conversation: body.history || [] }; const result = await askDeepSeek(`卖家当前供给档案：${JSON.stringify(body.profile || {})}\n历史对话：${JSON.stringify(body.history || [])}\n本轮消息：${String(body.message || '')}`, sellerContext);
    if (!result.answer) return sendJson(res, 503, { error: 'DeepSeek 暂时没有返回，请检查 API Key、网络和服务日志。', provider: result.provider, fallback_reason: result.reason });
    return sendJson(res, 200, { answer: result.answer, provider: 'deepseek', model: result.model });
  }
  if (req.method === 'POST' && path === '/api/v1/agent/chat') {
    const user = userFromRequest(req); if (!user) return sendJson(res, 401, { error: '请先登录后使用 AI 对话' });
    const body = await readBody(req);
    const opportunity = state.opportunities[String(body.opportunity_id || '')];
    if (!opportunity || !canAccess(user, opportunity)) return sendJson(res, 403, { error: '无权访问当前买家上下文' });
    const result = await askDeepSeek(String(body.message || ''), opportunity);
    return sendJson(res, 200, { answer: result.answer, provider: result.provider, model: result.model, fallback_reason: result.reason, opportunity_id: opportunity.id });
  }
  if (req.method === 'GET' && path === '/api/v1/opportunities') { const user = userFromRequest(req); if (!user) return sendJson(res, 401, { error: '请先登录' }); const rows = Object.values(state.opportunities).filter(opportunity => canAccess(user, opportunity)); return sendJson(res, 200, rows.map(opportunity => projectOpportunity(opportunity, user.role))); }
  const resumeMatch = path.match(/^\/api\/v1\/agent\/runs\/([^/]+)\/resume$/);
  if (req.method === 'POST' && resumeMatch) {
    const user = userFromRequest(req); const payload = await readBody(req); const previous = state.runs[resumeMatch[1]];
    if (!user || !previous) return sendJson(res, 404, { error: 'Run 不存在或未登录' });
    const opportunity = state.opportunities[previous.opportunity_id]; if (!canAccess(user, opportunity)) return sendJson(res, 403, { error: '无权恢复这笔 Run' });
    if (!payload.idempotency_key) return sendJson(res, 400, { code: 'IDEMPOTENCY_KEY_REQUIRED', error: '恢复 Run 必须提供 idempotency_key' });
    return respond(res, await createRun({ ...payload, opportunity_id: previous.opportunity_id, event_type: 'MANUAL_RESUME' }, user));
  }
  return controlHandler(req, res, path);
};
const mime = { '.html': 'text/html; charset=utf-8', '.js': 'text/javascript; charset=utf-8', '.css': 'text/css; charset=utf-8', '.json': 'application/json; charset=utf-8' };
const baseLoadState = loadState;
loadState = async function() { await baseLoadState(); state.products ||= [{ id: 'sku-matcha-demo', name: '贵州抹茶粉 · 饮品级', markets: 'US, JP, EU, UK, AU', certs: 'USDA Organic · HACCP · JAS', status: '已上架' }]; try { const imported = await loadFreeOpportunities(); for (const opportunity of imported) state.opportunities[opportunity.id] = opportunity; state.free_data_source = 'origin/Free'; } catch (error) { state.free_data_source = 'fallback'; state.free_data_error = error.message; } };
await loadState();
const server = createServer(async (req, res) => { const url = new URL(req.url, `http://${req.headers.host || 'localhost'}`); try { if (req.method === 'GET' && url.pathname === '/api/health') return sendJson(res, 200, { ok: true, provider: process.env.DEEPSEEK_API_KEY ? 'deepseek-ready' : 'rules-fallback', agent_version: AGENT_VERSION, model: DEEPSEEK_MODEL }); if (req.method === 'GET' && url.pathname === '/api/public/opportunities') return sendJson(res, 200, Object.values(state.opportunities).filter(item => item.id.startsWith('opp-')).map(item => ({ ...item, source: state.free_data_source || 'origin/Free' }))); if (url.pathname.startsWith('/api/v1/auth/')) return authHandler(req, res, url.pathname); if (url.pathname.startsWith('/api/v1/')) return handleV1(req, res, url.pathname); if (req.method !== 'GET') return sendJson(res, 405, { error: 'Method not allowed' }); const file = url.pathname === '/' ? 'index.html' : normalize(url.pathname).replace(/^[/\\]+/, ''); const filePath = join(ROOT, file); if (!filePath.startsWith(ROOT)) return sendJson(res, 403, { error: 'Forbidden' }); const content = await readFile(filePath); res.writeHead(200, { 'Content-Type': mime[extname(filePath)] || 'application/octet-stream' }); res.end(content); } catch (error) { sendJson(res, 500, { error: error.message }); } });
server.listen(PORT, () => console.log(`QianPulse Agent API: http://localhost:${PORT}`));
