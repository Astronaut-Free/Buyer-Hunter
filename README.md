# Buyer Hunter MVP

静态首页原型，直接打开 `index.html` 即可预览。

## GitHub 上传

1. 打开仓库的 `MVP` 分支。
2. 选择 `Add file` -> `Upload files`。
3. 上传 `buyer-hunter-mvp` 文件夹中的 `index.html` 和本文件，或上传 ZIP 后在本地解压再上传。

页面中的图片区域均为留白占位，可按提示替换为最终图片资源。
