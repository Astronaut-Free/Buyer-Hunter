# 20 · 第二轮缺口修复（A5/排名/a8/NL/Phase 4/实网）— 交接文档

> 交接时间：2026-08-29 · 交接人：Claude（buyer-hunter-integration-41，主会话）
> 状态：**3 个提交已落地 + 工作树大量未提交改动；用户决定本次不提交**
> 协作会话：buyer-hunter-integration-81（demo 停用 + 实时采集面板）——其交接见 [docs/19](19_前端整合与实时采集_交接.md)
> 用户最终决定：**暂停提交，工作树保持现状**。本文档用于后续恢复时无缝接续。

## 1. 总览（两轮工作全景）

| 轮次 | 状态 | 内容 |
|---|---|---|
| 第一轮 | ✅ 已推送 origin/main = d17d41b | UI 再整合（origin/ui @ c7d5634）、门户接实时 API、双向数据桥、domain 实体解析、INTERNAL 邀请码注册；12 项审计全绿 |
| 第二轮 | 🔶 部分提交，部分在工作树 | 全部代码缺口 + 实网 smoke（见下）；**未推送** |

**第二轮已提交（integration 分支 HEAD = a7a0de5，领先 origin/main 3 个提交）**：

| 提交 | 内容 | 测试 |
|---|---|---|
| ec33120 | **A5 风险深度**：从 impl-b 文件级移植 `risk_items_v1.py`（9 类）+ `buyer_profile_v1.py`，迁移 `005_buyer_identity.sql`；**新增 4 类启发式**（CREDIT_UNKNOWN / FRAUD_SIGNAL / IP_CONFLICT / CONTRACT_RISK，13 类 total）；builder 内联 5 项换成 `classify_risk_items`；run_a5 + a5.js 镜像补深度检查；SKILL.md + docs/15 修正 | pytest 165 ✓ |
| 0831aa5 | **A2↔A1 排名联动**：迁移 `006_promotion_bonus.sql`（opportunity_decision += promotion_bonus）；import 脚本幂等重算（WON+15 / NEGOTIATING+10 / 已链接 a2_rank≥70 +10，取最大）；/today ORDER BY (opportunity_score+promotion_bonus)，summary 暴露 promotion_bonus | pytest 170 ✓ |
| a7a0de5 | **Phase 8（qianpulse.a8.deal_action）**：capability_cli.py + a8.js（BLOCK→HALT 永不绕过；PURSUE_NOW→OUTREACH / VERIFY_FIRST→VERIFY_GAPS / WATCH→SCHEDULE_REVIEW / PASS→NO_ACTION；无快照→NOT_APPLICABLE 不拖死 A6）；dependency-refresh 给 a8 附决策快照上下文；routeA6ChangedFields 失效 a8；selectA6NextAction 仅在 HALT/SCHEDULE_REVIEW 采纳、A6 保持 next_action 唯一所有权；registry 7 条、skill 调度审计 6/6 | pytest 175 ✓ / node 155+1(peer) ✓ |

## 2. 工作树中未提交的改动（按文件归属）

### 我的文件（本会话，未提交）

| 文件 | 状态 | 说明 |
|---|---|---|
| `agent/providers/deepseek.js`（新） | ✅ 完成 + 测试 | DeepSeek chat client（createJsonClient + withRetry，json_object，失败抛错→规则兜底） |
| `agent/skill-runtime/nl-target-parser.js`（新） | ✅ 完成 + 测试 | 规则解析器（国家/品类/买家类型/约束词表）+ `buildNlTargetPayload` 生成标准 /runs payload |
| `agent/tests/nl-target.test.js`（新） | ✅ 10/10 绿 | 解析器 6 + DeepSeek client 3（注入 fetchImpl）+ 端点全流程 1（401/403/400/200/422） |
| `agent/server/sqlite-opportunity-store.js`（新） | 🔶 完成未接线未测试 | Phase 4 写穿透 store（node:sqlite DatabaseSync，每操作开/关，WAL+busy_timeout；duck API：loadAll/get/list/upsertSeed/applyA6Envelope/bindExternalRef/resolveExternalRef；A2 行合成 buyer+signal 满足 FK；A6 outcome 直写 deal_outcome）。**语法已过 node --check，尚未接进 index.js，尚无测试** |
| `db/migrations/007_core_opportunity_unification.sql`（新） | ✅ 完成 | opportunity += origin/seed_key/agent_stage/agent_status/agent_fields_json/a2_json/seller_json/a6_json/outreach_json/contact_json/agent_updated_at + origin 索引 |
| `pipeline/promotion_v1.py`（新） | ✅ 完成 | 排名联动的独立归属模块（compute/apply_promotions）——C5 删桥后 promotion 的家 |
| `scripts/import_agent_outcomes.py` | ⚠️ 有重复待清 | 顶部已加 `from promotion_v1 import apply_promotions`，但**本地旧版 `compute_promotions`/`apply_promotions`（237/265 行）还在，本地定义遮蔽 import**（功能等价、测试全绿，但必须删掉本地重复）。上一轮被用户拒绝的那次编辑就是要删它们——恢复工作时先做这个 |

### 双方共有文件（hunk 分流，未提交）

| 文件 | 我的 hunk | 对方 hunk |
|---|---|---|
| `agent/server/index.js` | 22–23 行：`createDeepSeekClient` / `parseNlTarget,buildNlTargetPayload` import；727 行起：`POST /api/v1/agent/nl-targets` 路由（401/403/400/422/200，DeepSeek 优先、规则兜底、只返回 payload 不自动执行） | REPO_ROOT 常量、collection_runs 状态槽、refreshFreeOpportunities 抽取、collectionRunner 实例化 + markInterrupted、collection-runs 三条路由（对方已冻结，特征见 docs/19 §6） |

### 对方的文件（勿动，见 docs/19）

`agent/server/collection-runner.js`、`agent/tests/collection-runner.test.js`、`agent/index.html`、`run.ps1`、`Makefile`、`site/nav-bridge.js`、`pipeline/run_pipeline.py`（[START] 行）、`docs/16`、`agent/README.md`、`design-qa.md`、`site/PROVENANCE.md`、`docs/19`。

### 约定由我收尾（对方不碰）

`README.md`、`docs/18_整合架构与运行.md`、`scripts/audit.py`——需要把：采集面板事实（对方提供）、a8、NL 入口、统一 store、smoke 第 13 项全部写进矩阵与审计。

## 3. 当前测试状态（2026-08-29 实测）

- **pytest**：175 passed, 1 skipped ✅
- **node**：156 tests，155 pass，**1 fail = 对方的 collection-runner.test.js:189 偶发 PENDING**（隔离跑 13/13 绿；对方 §4 遗留 2 已记录，建议双方改动稳定后全量复跑）
- **skill 调度审计**：6/6 派发（a8 计入），python 委托 A3/A4/A5/A8 全 python ✅
- 全量 `python scripts/audit.py` 自第二轮起**未跑过**（收尾时跑）

## 4. C5（Phase 4 统一 store）剩余步骤——恢复工作时的执行顺序

1. **清理 import_agent_outcomes.py 本地重复 promotion 函数**（被拒绝的那个编辑）
2. **builder 接 007**：`executescript 007`；A1 INSERT 补 origin='A1_INBOUND_DEMAND' / seed_key='bridge:free:<id>' / agent_stage='CONTACTED' 三列
3. **builder 重建保全**（关键设计点）：`preserve_agent_state(old_db, conn)` —— 旧库存在且含 origin 列时，把 origin='A2_PROACTIVE' 的 opportunity + 其 buyer/signal 行 + deal_outcome + buyer_alias + entity_merge_audit + agent_discovered_target 拷进新库（INSERT OR IGNORE，A2 id 命名空间无碰撞）；随后调 `promotion_v1.apply_promotions`。旧库无 origin 列（Phase 4 前）则跳过
4. **index.js boot 接线**（在 peer 结构上增量）：loadCoreOpportunities() 从 SQLite 载入替换 loadFreeOpportunities/merge-on-reload；A2/A6 写走 SqliteOpportunityStore（getState 指向 state）；保留 agent-state.json 只存 runtime.* + auth；`AGENT_DB_FILE` env 覆盖；engines.node ">=22.5"
5. **删桥**：export/import 两脚本 + 两测试 + audit 两个桥检查 + run.ps1 Invoke-Export/Import + Makefile export/import + repository 桥函数 + agent-outcomes 机制 + reverse-bridge.test.js + .gitignore 行；**同步改 peer 的 collection-runner.js**（其采集后调用 export/import——改为只 reload；需与对方确认）
6. **审计换检查**："unified core.opportunity store" 替代两个桥检查；contracts/opportunity-bridge-v1.md 加 superseded 横幅；opportunity-v2.md → implemented；README/docs18 收尾（含对方采集面板事实）
7. **C6 实网 smoke**：`agent/scripts/provider-smoke.mjs`（Trademo searchBuyers / Apollo searchPeople / Smartlead getCampaignSequences 只读 + 可选 DeepSeek）；audit 第 13 项凭据在→真跑、无→SKIP；**执行时需向用户索取凭据**（SMARTLEAD_* / APOLLO_API_KEY / TRADEMO_*，可选 DEEPSEEK_API_KEY）
8. 全量 pytest + npm test + audit → 提交（按 hunk 分流）→ `git push origin integration:main`（快进）

## 5. 协作边界（与 docs/19 §6 一致）

- 我已冻结不再改：`agent/skill-runtime/a8.js`、`a6.js`、`dependency-refresh.js`、`python-capability-runners.mjs`、`registry.js`、`index.js`（barrel）、`agent/qianpulse-skill-orchestrator.js`、`agent/scripts/skill-dispatch-audit.mjs`、`scripts/capability_cli.py`（A5/A8 均已提交，不会再动，除 C5 无交集）
- 恢复后我会动：`pipeline/build_opportunity_store_v1.py`（007/保全）、`scripts/import_agent_outcomes.py`（清理+C5 删除）、`agent/server/index.js`（boot 接线，在 peer 结构上增量）、`agent/server/repository.js`（C5 删桥函数）、`agent/tests/*`（sqlite store 测试新增、reverse-bridge 删除）、`scripts/audit.py`、`README.md`、`docs/18`
- 提交建议：恢复后先用 `git add <明确文件清单>`（或 `git add -p`）分流，双方各提交各的；index.js 由一方整体提交前需把两方 hunk 都核对一遍（我的 hunk 特征见 §2）

## 6. 风险点清单

1. **import_agent_outcomes.py 重复函数**（§2）——恢复第一步处理，避免删除脚本时误删 import 依赖
2. **index.js 混 hunk**——对方已冻结；我的 hunk 只有 2 处（import + nl-targets 路由），分流时核对
3. **builder 原子替换会毁 agent 数据**——007 接上但保全步骤没做之前，**绝不能跑 `-Build`/`-Up`**（当前工作树 builder 不执行 007，暂无此风险；保全实现后才有）
4. **collection-runner 调桥脚本**——C5 删桥必须同步改 peer 的 runner（采集后热刷新逻辑由对方 reloadFree 承载）
5. **实网 smoke 依赖用户凭据**——尚未索取；无凭据时审计 SKIP 不挂
6. **smoke 前不得跑 `-Up` 全量采集**——对方 §4 遗留 1（`\undefined` 桥脚本失败）尚未定位，与我的删桥改动交错后更难排查，建议先按对方 checklist 排查再动 C5

## 7. 交接后立即要做的（按优先级）

1. 对方 §4 遗留 1（`\undefined` 桥脚本失败）定位——干净重启确认 3317 归属与 env
2. 我方：删 import_agent_outcomes.py 本地重复函数（被拒编辑重放）→ pytest 复跑
3. 全量 `npm test` 复跑确认对方 PENDING 偶发是否仍在
4. 按 §4 顺序继续 C5
