# 整合仓库审计报告

> 生成时间：2026-08-29T10:11:01+00:00　·　分支：`integration`　·　结论：**全部通过**

## 1. 硬性检查

| 检查项 | 结果 | 详情 |
|---|---|---|
| python test suite | ✅ PASS | 115 passed, 1 skipped |
| agent test suite | ✅ PASS | 127/127 passed |
| A1-A5 pipeline -> decision store | ✅ PASS | 51 decisions (18 non-PASS), 51 buyers, 51 SKU-fit rows |
| decision API | ✅ PASS | /health=ok, today returned 5 items, /decision[opp-33078eb1] ok=True |
| Free -> agent bridge | ✅ PASS | 18 rows exported (db non-PASS = 18) |
| reverse bridge (agent -> Free) | ✅ PASS | deal_outcome 0->1, targets 0->1, replay idempotent |
| agent runtime boot | ✅ PASS | a2_a6_runtime=ready, 8 capabilities |
| A2-A6 skill dispatch | ✅ PASS | registry=True routing=True envelopes=True e2e=True (5/5 capabilities dispatched) |
| Python capability CLI (A3/A4/A5 -> Free) | ✅ PASS | available=True; A3/A4/A5 source=python; live A6 cycle A4 refresh source=python |
| .agents/skills orchestrator specs | ✅ PASS | 6 skills, frontmatter-missing=none, doc-unreferenced=none |
| landing site (site/ <- origin/ui) | ✅ PASS | 2 pages, assets ok=True, nav-bridge wired=True, http 200=True |
| portal live wiring (CORS + opportunities-live.js) | ✅ PASS | CORS 4180=True, live js wired=True |

## 2. A2–A6 skill 调度

| capability | 调度 | envelope 有效 | 记为 Step | run_status |
|---|---|---|---|---|
| `qianpulse.a2.proactive_buyer_development` | ✅ | ✅ | ✅ | DONE |
| `qianpulse.a3.purchase_timing` | ✅ | ✅ | ✅ | MORE_EVIDENCE |
| `qianpulse.a4.supply_match` | ✅ | ✅ | ✅ | DONE |
| `qianpulse.a5.trade_risk` | ✅ | ✅ | ✅ | DONE |
| `qianpulse.a6.opportunity_progression` | ✅ | ✅ | ✅ | DONE |

- registry：True　routing：True　envelopes：True　e2e：True
- 事件路由：`SELLER_PROACTIVE_DEVELOPMENT`/`SYSTEM_NEW_PROSPECT_SIGNAL`/`PRE_REPLY_FOLLOWUP_DUE` → A2；`BUYER_MESSAGE`/`QUOTE_UPDATED`/`SAMPLE_UPDATED`/`APPROVAL_RESULT`/`EVIDENCE_ADDED`/`MANUAL_RESUME` → A6
- E2E：A2 派发 → 生成 Opportunity → 买家回复 → A6 派发 → 自动刷新 A4/A5/A3 → 每步记为 AgentStep → 生成 Checkpoint

## 3. 模块完成度

| 模块 | 运行时 | 状态 | 已实现 | 缺口 |
|---|---|---|---|---|
| **A1 全球需求捕捉** | Python — pipeline/collect_*.py, clean_and_score_*, aggregate_* | 运行中 | 51 signals -> 51 decisions；多平台采集器 + 四维验真 + 实体去重；committed fixture 支持离线重建 | 买方法定主体解析未完成（QUALIFIED_PENDING_ENTITY）；部分源精准命中率低；hktdc/jetro 采集器在 Free 上未提交 |
| **A2 主动商机拓展** | Node — agent/skill-runtime/a2*.js + agent/server/a2a6-live-runtime.js | sandbox 闭环 | Target -> Buyer Company Discovery -> Buyer Fit(证据门槛) -> Contact -> Readiness Gate -> Email Draft -> Human Gate -> Smartlead 队列；skill 调度审计通过 | 自然语言入口未接线（只吃结构化 target）；发现源仅 Trademo 1 个；实网待 5 个凭据 |
| **A3 采购时机判断** | Python 权威（timing_score + buying_window_fields）· Node fallback | Python 权威 | A6 会话内刷新经 capability CLI 调 Free 的 timing_score；recency + urgency + stage + continuity − staleness；Python 不可用时自动回退 Node placeholder | 未做统计校准；子进程启动 ~200ms/次 |
| **A4 贵州供需匹配** | Python 权威（supply_demand_fit_v1.py 逐 SKU）· Node fallback | Python 权威 | 51 条逐 SKU 评估入库；A6 会话内刷新经 capability CLI 拿到真实 supply_pool_status / best_verdict / summary_zh；硬条件先于软条件，MATCH/CONDITIONAL/BLOCK | 卖家目录是 demo 数据（3 卖家/5 SKU），需真实卖家入库 |
| **A5 智能匹配风控** | Python 权威（market_access_score + 目的地/allowed/blocked/支付 审查）· Node fallback | 部分（准入已归 Python） | A6 会话内刷新经 capability CLI 调 Free；目的地黑名单 → BLOCKED，缺政策 → MORE_EVIDENCE，否则 REVIEWED + market_access 分 | 买家信用 / 欺诈预警 / 知识产权 / 合同履约 未做；法规 Provider 未接 |
| **A6 成交自动推进** | Node — agent/skill-runtime/a6*.js + 两趟判断 + dependency gate | sandbox 闭环 | 16 意图 taxonomy + 11 阶段机 + 15 动作 taxonomy；买家回复 -> 变更字段 -> 自动刷新 A3/A4/A5 -> 证据安全草稿 -> Human Gate；高风险强制人工；skill 调度审计通过 | 实网回复回路未验证（缺 Smartlead 凭据）；LLM 判断层未加（仍规则化） |
| **机会决策引擎 / 读接口 / Demo** | Python — opportunity_decision_engine_v1.py + api/app.py + demo/(React) | 就绪 | 权重 30/30/20/10/10，truth 门槛 60，ruleset_version + input_snapshot_sha256；FastAPI 4 端点；demo 可构建 | 会员/Lead Access 为 demo 模拟；未接支付 |
| **门户站点 (landing)** | 静态 — site/(index.html + opportunities.html)，vendored 自 origin/ui @ c7d5634 | 就绪(前门) | WebGL/canvas 首页 + 全球商机展示页（network-stage + deal-section）；离线地图数据自带；nav-bridge.js 把登录/CTA 指向 demo；opportunities-live.js 把精选卡片/需求行/KPI 换成 /api/v1 实时数据（API 失败静默回退静态样例，LIVE/FALLBACK 徽标）；run.ps1 -Up / make up 起在 4180 | 作者 yayaw2826-oss 仍在 origin/ui 迭代（re-sync 时需重加两行 script） |
| **Agent 控制面** | Node — AgentRun / Step / Checkpoint / Approval / Trace / Idempotency | 就绪 | 每个 capability 调用记为 Step；四层幂等；INTERNAL 可观测性；registry + 事件路由审计通过；INTERNAL 注册经 INTERNAL_INVITE_CODE 邀请码开放 | 控制面与 Free 采集为文件桥（非实时 API 写回） |
| **数据桥 (Free <-> agent)** | Python — scripts/export_opportunities_for_agent.py + scripts/import_agent_outcomes.py · Node — persist 写 agent-outcomes.json + boot 时 merge-on-reload | 就绪(v2 双向) | 正向：决策 store -> agent/db/opportunities.json（非 PASS 行，证据 URL 可回溯）；反向：A6 结果 -> deal_outcome、A2 目标 -> agent_discovered_target，幂等 upsert，重建后可重放；domain 实体解析自动建 buyer_alias + entity_merge_audit（AUTO_MERGE）并在 agent 侧绑定 free:buyer 引用 | 实网 A6 回流待 Smartlead 凭据；实体合并待 buyer domain 填充生效（当前 51 买家均无 domain） |
| **Capability 合同 + Python 桥** | scripts/capability_cli.py（stdin/stdout）· agent/skill-runtime/python-capability-runners.mjs | 就绪 | 统一 CapabilityResultEnvelope / AgentEvent JSON Schema + 两侧合同测试；A6 依赖刷新经 execFileSync 调 Python，任何失败自动回退 Node；QIANPULSE_PYTHON_CAPABILITIES=off 可关 | 子进程启动延迟；后续可升级为常驻 HTTP capability 服务 |
| **统一 Opportunity（v2）** | contracts/opportunity-v2.md（设计） | 设计完成 · 未实现 | core.opportunity（origin A1/A2）+ intel.decision_snapshot + runtime.agent_* 的目标形状与迁移草案 | Phase 4：Free SQLite 与 MVP agent-state 仍是两套主键；下一增量 |

## 4. 如何复跑

```bash
python scripts/audit.py          # 本报告
python -m pytest -q              # Python 测试
cd agent && npm test            # Node 测试
node agent/scripts/skill-dispatch-audit.mjs   # 仅 skill 调度
python scripts/import_agent_outcomes.py       # 反向桥（幂等）
```

## 5. 不在整合范围（后续）

- 单语言统一（Node ↔ Python 移植）
- 统一 Opportunity 主键（Phase 4，设计见 `contracts/opportunity-v2.md`）
- A2 自然语言入口接线
- 实网 provider smoke（Smartlead / Apollo / Trademo 凭据）
- 控制面实时反向写回（当前为文件桥，非 API）
- `brand2` 前端增量 cherry-pick（联系方式解锁双态 UX）
