# 整合仓库审计报告

> 生成时间：2026-08-29T05:05:45+00:00　·　分支：`integration`　·　结论：**全部通过**

## 1. 硬性检查

| 检查项 | 结果 | 详情 |
|---|---|---|
| python test suite | ✅ PASS | 100 passed, 1 skipped |
| agent test suite | ✅ PASS | 109/109 passed |
| A1-A5 pipeline -> decision store | ✅ PASS | 51 decisions (18 non-PASS), 51 buyers, 51 SKU-fit rows |
| decision API | ✅ PASS | /health=ok, today returned 5 items, /decision[opp-33078eb1] ok=True |
| Free -> agent bridge | ✅ PASS | 18 rows exported (db non-PASS = 18) |
| agent runtime boot | ✅ PASS | a2_a6_runtime=ready, 8 capabilities |
| A2-A6 skill dispatch | ✅ PASS | registry=True routing=True envelopes=True e2e=True (5/5 capabilities dispatched) |
| .agents/skills orchestrator specs | ✅ PASS | 6 skills, frontmatter-missing=none, doc-unreferenced=none |

## 2. A2–A6 skill 调度

| capability | 调度 | envelope 有效 | 记为 Step | run_status |
|---|---|---|---|---|
| `qianpulse.a2.proactive_buyer_development` | ✅ | ✅ | ✅ | DONE |
| `qianpulse.a3.purchase_timing` | ✅ | ✅ | ✅ | MORE_EVIDENCE |
| `qianpulse.a4.supply_match` | ✅ | ✅ | ✅ | DONE |
| `qianpulse.a5.trade_risk` | ✅ | ✅ | ✅ | DONE |
| `qianpulse.a6.opportunity_progression` | ✅ | ✅ | ✅ | DONE |

- registry：True　routing：True　envelopes：True　e2e：True
- 事件路由：`SELLER_PROACTIVE_DEVELOPMENT`/`SYSTEM_NEW_PROSPECT_SIGNAL`/`PRE_REPLY_FOLLOWUP_DUE` → A2；`BUYER_MESSAGE`/`QUOTE_UPDATED`/`SAMPLE_UPDATED`/`APPROVAL_RESULT`/`EVIDENCE_ADDED`/`MANUAL_RESUME` → A6
- E2E：A2 派发 → 生成 Opportunity → 买家回复 → A6 派发 → 自动刷新 A4/A5/A3 → 每步记为 AgentStep → 生成 Checkpoint

## 3. 模块完成度

| 模块 | 运行时 | 状态 | 已实现 | 缺口 |
|---|---|---|---|---|
| **A1 全球需求捕捉** | Python — pipeline/collect_*.py, clean_and_score_*, aggregate_* | 运行中 | 51 signals -> 51 decisions；多平台采集器 + 四维验真 + 实体去重；committed fixture 支持离线重建 | 买方法定主体解析未完成（QUALIFIED_PENDING_ENTITY）；部分源精准命中率低；hktdc/jetro 采集器在 Free 上未提交 |
| **A2 主动商机拓展** | Node — agent/skill-runtime/a2*.js + agent/server/a2a6-live-runtime.js | sandbox 闭环 | Target -> Buyer Company Discovery -> Buyer Fit(证据门槛) -> Contact -> Readiness Gate -> Email Draft -> Human Gate -> Smartlead 队列；skill 调度审计通过 | 自然语言入口未接线（只吃结构化 target）；发现源仅 Trademo 1 个；实网待 5 个凭据 |
| **A3 采购时机判断** | Python(决策引擎 timing_score) + Node(a3 refresh) | 规则版就绪 | recency + urgency + stage + continuity − staleness；A6 变更字段触发按需刷新，skill 调度通过 | 两套实现（Python 权威、Node refresh 用于会话内）；未做统计校准 |
| **A4 贵州供需匹配** | Python — supply_demand_fit_v1.py（逐 SKU）+ Node(a4 refresh) | 就绪 | 51 条逐 SKU 评估；硬条件先于软条件，MATCH/CONDITIONAL/BLOCK | 卖家目录是 demo 数据（3 卖家/5 SKU），需真实卖家入库 |
| **A5 智能匹配风控** | Python(market_access + 硬冲突) + Node(a5 trade_risk refresh) | 部分 | 市场准入评分 + 真实性门槛 + 硬冲突 blocker；目的地/支付变更触发 A5 刷新 | 买家信用 / 欺诈预警 / 知识产权 / 合同履约 未做；实体去重规则部分实现 |
| **A6 成交自动推进** | Node — agent/skill-runtime/a6*.js + 两趟判断 + dependency gate | sandbox 闭环 | 16 意图 taxonomy + 11 阶段机 + 15 动作 taxonomy；买家回复 -> 变更字段 -> 自动刷新 A3/A4/A5 -> 证据安全草稿 -> Human Gate；高风险强制人工；skill 调度审计通过 | 实网回复回路未验证（缺 Smartlead 凭据）；LLM 判断层未加（仍规则化） |
| **机会决策引擎 / 读接口 / Demo** | Python — opportunity_decision_engine_v1.py + api/app.py + demo/(React) | 就绪 | 权重 30/30/20/10/10，truth 门槛 60，ruleset_version + input_snapshot_sha256；FastAPI 4 端点；demo 可构建 | 会员/Lead Access 为 demo 模拟；未接支付 |
| **Agent 控制面** | Node — AgentRun / Step / Checkpoint / Approval / Trace / Idempotency | 就绪 | 每个 capability 调用记为 Step；四层幂等；INTERNAL 可观测性；registry + 事件路由审计通过 | INTERNAL 用户无法自注册（需手工种子或环境变量）；控制面与 Free 采集为单向桥 |
| **数据桥 (Free -> agent)** | Python — scripts/export_opportunities_for_agent.py | 就绪(v1 单向) | 决策 store -> agent/db/opportunities.json；非 PASS 行；证据 URL 可回溯；6 个测试 | 单向；A2 发现与 A6 结果不回流；无 domain 实体合并 |

## 4. 如何复跑

```bash
python scripts/audit.py          # 本报告
python -m pytest -q              # Python 测试
cd agent && npm test            # Node 测试
node agent/scripts/skill-dispatch-audit.mjs   # 仅 skill 调度
```

## 5. 不在整合范围（后续）

- 单语言统一（Node ↔ Python 移植）
- A2 自然语言入口接线
- A2↔A1 双向汇合（被开发公司之后发 RFQ 自动升进）
- 实网 provider smoke（Smartlead / Apollo / Trademo 凭据）
- INTERNAL 用户种子 / 控制面反向写回
