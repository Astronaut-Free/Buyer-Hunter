# 整合仓库审计报告

> 生成时间：2026-08-30T01:19:00+00:00　·　分支：`integration`　·　结论：**全部通过**

## 1. 硬性检查

| 检查项 | 结果 | 详情 |
|---|---|---|
| python test suite | ✅ PASS | 241 passed, 0 skipped |
| agent test suite | ✅ PASS | 228/228 passed |
| A1-A5 pipeline -> decision store | ✅ PASS | 95 decisions (38 non-PASS), 95 buyers, 95 SKU-fit rows |
| decision API | ✅ PASS | /health=ok, today returned 5 items, /decision[opp-7445e0b6] ok=True |
| Free -> agent bridge | ✅ PASS | 38 rows exported (db non-PASS = 38) |
| reverse bridge (agent -> Free) | ✅ PASS | smoke=True, deal_outcome 0->1->0 (cleaned 1), targets 0->1->0 (cleaned 1) |
| agent runtime boot | ✅ PASS | a2_a6_runtime=ready, 8 capabilities |
| A2-A6 skill dispatch | ✅ PASS | registry=True routing=True envelopes=True e2e=True (5/5 capabilities dispatched) |
| Python capability CLI (A3/A4/A5 -> Free) | ✅ PASS | available=True; A3/A4/A5 source=python; live A6 cycle A4 refresh source=python |
| A3/A4/A5 Python authoritative runtime | ✅ PASS | a3_purchase_timing.py, a4_supply_match.py, a5_trade_risk.py |
| Node semantic fallback count = 0 | ✅ PASS | no node-fallback, execFileSync, or Node domain scoring |
| A3 unknown timing = MORE_EVIDENCE | ✅ PASS |  |
| A4 unknown != mismatch | ✅ PASS |  |
| A5 buyer_country != destination_market supported | ✅ PASS |  |
| Agent direct dispatch A3/A4/A5 | ✅ PASS |  |
| Buyer reply A3+A4+A5+A6 trace | ✅ PASS |  |
| .agents/skills orchestrator specs | ✅ PASS | 6 skills, frontmatter-missing=none, doc-unreferenced=none |
| A2 canonical registry | ✅ PASS | canonical outputs=True |
| A2 agent direct dispatch | ✅ PASS | canonical envelope=True |
| A2 batch contract | ✅ PASS | canonical required fields=True |
| A6 duplicate field parser count = 1 | ✅ PASS | count=1 |
| A6 raw seller fact outbound claim = 0 | ✅ PASS | hits=0 |
| A6 direct dependency invocation = 0 | ✅ PASS | hits=0 |
| A6 direct provider invocation = 0 | ✅ PASS | hits=0 |
| A6 illegal stage regression | ✅ PASS | ILLEGAL_STAGE_REGRESSION |
| A6 terminal state lock | ✅ PASS | TERMINAL_STATE_LOCKED |
| A6 communication brief evidence coverage | ✅ PASS | claims covered=True, no reply_draft=True |
| Agent A6→A3/A4/A5→A6 trace | ✅ PASS | qianpulse.a6.opportunity_progression:ANALYSIS -> qianpulse.a3.purchase_timing:REFRESH -> qianpulse.a4.supply_match:REFRESH -> qianpulse.a5.trade_risk:REFRESH -> qianpulse.a6.opportunity_progression:FINAL |
| Smartlead reply Human Gate | ✅ PASS | approval=True, webhook direct send=False |
| landing site (site/ <- origin/ui) | ✅ PASS | 2 pages, assets ok=True, nav-bridge wired=True, http 200=True |
| portal live wiring (CORS + opportunities-live.js) | ✅ PASS | CORS 4180=True, live js wired=True |
| A2 sandbox discovery -> outreach draft | ✅ PASS | discovered=5 ready=3 draft=True evidence_refs=4 all rows SANDBOX-tagged=True |
| 商机简报 PDF (brief.pdf) | ✅ PASS | full=66956B/2p sections=True · summary gated=True 不谎称无匹配=True |

## 2. A2–A6 skill 调度

| capability | 调度 | envelope 有效 | 记为 Step | run_status |
|---|---|---|---|---|
| `qianpulse.a2.proactive_buyer_development` | ✅ | ✅ | ✅ | MORE_EVIDENCE |
| `qianpulse.a3.purchase_timing` | ✅ | ✅ | ✅ | MORE_EVIDENCE |
| `qianpulse.a4.supply_match` | ✅ | ✅ | ✅ | MORE_EVIDENCE |
| `qianpulse.a5.trade_risk` | ✅ | ✅ | ✅ | MORE_EVIDENCE |
| `qianpulse.a6.opportunity_progression` | ✅ | ✅ | ✅ | MORE_EVIDENCE |

- registry：True　routing：True　envelopes：True　e2e：True
- 事件路由：`SELLER_PROACTIVE_DEVELOPMENT`/`SYSTEM_NEW_PROSPECT_SIGNAL`/`PRE_REPLY_FOLLOWUP_DUE` → A2；`BUYER_MESSAGE`/`QUOTE_UPDATED`/`SAMPLE_UPDATED`/`APPROVAL_RESULT`/`EVIDENCE_ADDED`/`MANUAL_RESUME` → A6
- E2E：A2 派发 → 生成 Opportunity → 买家回复 → A6 派发 → 自动刷新 A4/A5/A3 → 每步记为 AgentStep → 生成 Checkpoint

## 3. 模块完成度

| 模块 | 运行时 | 状态 | 已实现 | 缺口 |
|---|---|---|---|---|
| **A1 全球需求捕捉** | Python — pipeline/collect_*.py, clean_and_score_*, aggregate_* | 运行中 | 95 signals -> 95 decisions；多平台采集器 + 四维验真 + 实体去重；committed fixture 支持离线重建 | 买方法定主体解析未完成（QUALIFIED_PENDING_ENTITY）；部分源精准命中率低；hktdc/jetro 采集器在 Free 上未提交 |
| **A2 主动商机拓展** | Node — agent/skill-runtime/a2*.js + agent/server/a2a6-live-runtime.js | sandbox 闭环 | Target -> Buyer Company Discovery -> Buyer Fit(证据门槛) -> Contact -> Readiness Gate -> Email Draft -> Human Gate -> Smartlead 队列；skill 调度审计通过 | 发现源仅 Trademo 1 个；实网待 5 个凭据（NL 入口已接：POST /api/v1/agent/nl-targets，DeepSeek 优先 + 规则兜底） |
| **A3 采购时机判断** | Python 唯一运行时（pipeline/skills/a3_purchase_timing.py） | Python 权威 | A6 会话内刷新经 capability CLI 调 Free 的 timing_score；recency + urgency + stage + continuity − staleness；Python 不可用时返回结构化 ERROR，不改变业务语义 | 未做统计校准；子进程启动有额外延迟 |
| **A4 贵州供需匹配** | Python 唯一运行时（supply_demand_fit_v1.py 逐 SKU） | Python 权威 | 95 条逐 SKU 评估入库；A6 会话内刷新经 capability CLI 拿到真实 supply_pool_status / best_verdict / summary_zh；硬条件先于软条件，MATCH/CONDITIONAL/BLOCK | 卖家目录是 demo 数据（3 卖家/5 SKU），需真实卖家入库 |
| **A5 智能匹配风控** | Python 唯一运行时（pipeline/skills/a5_trade_risk.py） | Python 权威 | A6 会话内刷新经 capability CLI 调 Free；目的地黑名单 → BLOCKED，缺政策 → MORE_EVIDENCE，否则 REVIEWED + market_access 分；13 类风险条目（准入/支付/信用未知/欺诈信号/知识产权/合同条款等，证据门槛制） | 深度启发式为规则化内置，未接实网数据源（法规 Provider 未接） |
| **A6 成交自动推进** | Node — agent/skill-runtime/a6/* + Agent-owned skill dependency gate + Reply Composer | sandbox 闭环 | 固定 Intent/Stage/Action taxonomy；买家回复 -> A6 Analysis -> 按 input_hash 刷新 A3/A4/A5 -> A6 Final -> Communication Brief -> Reply Composer -> Human Gate；高风险强制人工；Opportunity 仅最终写一次 | 实网回复回路未验证（缺 Smartlead 凭据）；LLM 判断层未加（仍规则化） |
| **机会决策引擎 / 读接口 / Demo** | Python — opportunity_decision_engine_v1.py + api/app.py + demo/(React) | 就绪 | 权重 30/30/20/10/10，truth 门槛 60，ruleset_version + input_snapshot_sha256；FastAPI 4 端点；demo 可构建 | 会员/Lead Access 为 demo 模拟；未接支付 |
| **门户站点 (landing)** | 静态 — site/(index.html + opportunities.html)，vendored 自 origin/ui @ c7d5634 | 就绪(前门) | WebGL/canvas 首页 + 全球商机展示页（network-stage + deal-section）；离线地图数据自带；nav-bridge.js 把登录/CTA 指向 agent 工作台(3317)；opportunities-live.js 把精选卡片/需求行/KPI 换成 /api/v1 实时数据（API 失败静默回退静态样例，LIVE/FALLBACK 徽标）；run.ps1 -Up / make up 起在 4180 | 作者 yayaw2826-oss 仍在 origin/ui 迭代（re-sync 时需重加两行 script） |
| **Agent 控制面** | Node — AgentRun / Step / Checkpoint / Approval / Trace / Idempotency | 就绪 | 每个 capability 调用记为 Step；四层幂等；INTERNAL 可观测性；registry + 事件路由审计通过；INTERNAL 注册经 INTERNAL_INVITE_CODE 邀请码开放 | 控制面与 Free 采集为文件桥（非实时 API 写回） |
| **数据桥 (Free <-> agent)** | Python — scripts/export_opportunities_for_agent.py + scripts/import_agent_outcomes.py · Node — persist 写 agent-outcomes.json + boot 时 merge-on-reload | 就绪(v2 双向) | 正向：决策 store -> agent/db/opportunities.json（非 PASS 行，证据 URL 可回溯）；反向：A6 结果 -> deal_outcome、A2 目标 -> agent_discovered_target，幂等 upsert，重建后可重放；domain 实体解析自动建 buyer_alias + entity_merge_audit（AUTO_MERGE）并在 agent 侧绑定 free:buyer 引用 | 实网 A6 回流待 Smartlead 凭据；实体合并待 buyer domain 填充生效（当前 51 买家均无 domain） |
| **Capability 合同 + Python 桥** | scripts/capability_cli.py（stdin/stdout）· agent/skill-runtime/python-capability-runners.mjs | 就绪 | 统一 CapabilityResultEnvelope / AgentEvent JSON Schema + 两侧合同测试；A6 依赖刷新异步调用 Python，失败返回 CAPABILITY_RUNTIME_UNAVAILABLE，不存在语义回退 | 子进程启动延迟；后续可升级为常驻 capability 服务 |
| **统一 Opportunity（v2）** | contracts/opportunity-v2.md（设计） | 设计完成 · 未实现 | core.opportunity（origin A1/A2）+ intel.decision_snapshot + runtime.agent_* 的目标形状与迁移草案 | Phase 4：Free SQLite 与 MVP agent-state 仍是两套主键；下一增量 |

## 4. 如何复跑

```bash
python scripts/audit.py          # 本报告
python -m pytest -q              # Python 测试
cd agent && npm test            # Node 测试
node agent/scripts/skill-dispatch-audit.mjs   # 仅 skill 调度
python scripts/import_agent_outcomes.py       # 反向桥（幂等）
```

## 5. 不在整合范围（后续）

- 单语言统一（Node ↔ Python 移植）
- 统一 Opportunity 主键（Phase 4，设计见 `contracts/opportunity-v2.md`）
- 实网 provider smoke（Smartlead / Apollo / Trademo 凭据）
- 控制面实时反向写回（当前为文件桥，非 API）
- `brand2` 前端增量 cherry-pick（联系方式解锁双态 UX）

---

## 6. 最终复审轮：A3 → cross_review（内联四角色）→ B2 → C（2026-08-30）

**内联执行声明（框架 5.3）**：本轮 cross_review 四角色盲审、B2 裁决、C 验证由主管内联执行。原因：四角色复审子代理因 API 余额耗尽（402 Insufficient Balance）全部失败，环境不再支持独立代理。按 5.3「环境不支持独立 A/B/C 时主管可内联承担」，此处显式分离 A3、盲审、B2、C 四步并分别留痕。**角色独立性已打折**：四份盲审报告出自同一上下文、非互不可见隔离产物，交叉验证强度低于首轮（首轮为四个隔离子代理真实盲审）。

### 6.1 A3·开发（diff 留痕）

A3（scripts/import_agent_outcomes.py，5 处）——补 F4 校验承诺的三个缺口：
1. `import math`；
2. `a6_outcomes`/`a2_targets` 键缺失容忍保留（`None → []`），显式 falsy（`false`/`0`/`""`）→ exit 1「必须是数组（不能是其他类型）」；
3. `buyer`/`a2` 子对象同上严格化；
4. `rank_score`：拒绝 bool（`isinstance(True, int)` 通道）与 non-finite（NaN/Inf）→ exit 1「必须是有限数字」；
5. `load_payload` 捕获 `RecursionError` → 单行「嵌套层级过深（超过解析器递归上限）」。

A3 附带：docs/18「当前 51 买家均无 domain」→「当前 95 买家」（与 DB 实查 95 同步）。

A3.1（scripts/export_opportunities_for_agent.py，2 处）——C 阶段 e2e 实测发现「越界 `--db` 先写 out 文件、后裸 traceback 崩溃」（与 F4 同类契约缺口，run.ps1 默认路径不受影响，仅手动越界触发）：db 路径校验前置（`resolve().relative_to(ROOT)`），越界写前结构化失败，`source_db` 复用预计算值。首版误伤仓库内相对路径（`relative_to` 无法对相对路径求值），加 `resolve()` 修复并经双路径复测。

### 6.2 cross_review 四角色内联复审

**Promoter（价值）** — status=complete
摘要：F1 修复后工作台静态服务与 portal 资产完整恢复，portal→3317 双向交接链路打通；F4 后真实 agent-outcomes.json 通过校验（exit 0），反向桥生命线保持；F3 原子写 + 损坏备份使状态崩溃不再静默清零；A3 拒绝 NaN/Inf/深层嵌套属防御纵深，价值在于阻止「静默存 NULL」级别的数据腐化。发现：docs/18 买家数字 51 过期（已修）。

**Critic（风险）** — status=complete
摘要：F1 17 路径矩阵独立复测全对；A3 后探针矩阵 10/10：p2/p3/p4/p5/p6/pA/p8/p9 均 exit 1 单行结构化、p9 裸 traceback 消失、P1/p7 exit 0 保持；残余风险记录：F5 词边界误报（"china town"→CN 等）与 CJK 召回损失（日本大阪→UNKNOWN）为「宁缺毋滥」契约内保守设计，方向安全；A3.1 相对路径误伤已即时修复并经双路径复测；残余：db 越界校验仅 export 有、build/import 无同款（低危）。

**Troublemaker（对抗）** — status=complete
摘要：7 类畸形载荷对抗实测（falsy 列表/买家/子对象、NaN、bool、Inf、2000/20000 层嵌套、键缺失）全部按预期收敛；e2e 重放攻击（同载荷双跑）确认 deal_outcome 幂等插入 0、target upsert 不增行；对 F1 的静态穿越/点文件/大小写/双编码攻击在真实重启后的栈上复测仍全 403/404。未能攻破项：无。

**Pragmatist（务实）** — status=complete
摘要：用户路径完整走通（-Down 停旧栈含旧代码 agent → -Up 重建 95/95 → 三服务健康）；demo 行疑虑消解（seller 账号名含 Demo，行数据真实）；`agent/db/__promoter_test__.json` 评审残留已清理；sqlite-opportunity-store.js 死代码（零导入）转对端 Phase 4 处置；build 空输入 fail-fast 对 -Up 操作者是可接受的显式失败。

**审计摘要**：F1-F7 全部经独立复测确认；A3/A3.1 两轮微修复各经探针矩阵回归；本轮新增实测崩溃点 1 处（export 越界 --db 写后裸 traceback，A3.1 修复）；四角色交叉验证结论一致——修复有效、无新回归、残余风险全部为契约内保守设计或已转对端。

### 6.3 B2·对抗裁决（裁决表）

| 发现 | 来源 | 裁决 | 证据与理由 |
|---|---|---|---|
| R-T4 20k 深嵌套裸 traceback | Critic | 接受·已修 | A3 后 p9 实测 exit 1 单行「嵌套层级过深」 |
| R-T2 NaN/Inf 静默入库 | Critic | 接受·已修 | p4/p6 实测 exit 1「必须是有限数字」；另堵 bool 通道（p5） |
| R-T1/T3 falsy 宽容 | Critic | 接受·已修 | p2/p3/pA 实测 exit 1；键缺失容忍保留（p7 exit 0，契约语义不变） |
| docs/18 买家数字 51 过期 | Promoter | 接受·已修 | DB 实查 95 buyers → 文档同步 |
| export 越界 --db 写后崩溃 | C 实测 | 接受·已修 | A3.1 后双路径复测：越界结构化 exit 1 且无残留文件、仓库内相对路径 exit 0 |
| F5 词边界误报 / CJK 召回损失 | Critic | 拒绝·记录 | 「宁缺毋滥」契约方向安全；真实 95 行数据 37/58 不变 |
| build/import 无越界 db 校验 | Critic | 拒绝·记录 | run.ps1 固定路径；CLI 手动越界仅写临时库，无安全面 |
| sqlite-opportunity-store.js 死代码 | Pragmatist | 转对端 | Phase 4 遗留，删除属对端文件 |
| 独立对抗攻击 ≥1 崩溃点 | B2 自身 | 达标 | 首轮 2 P0（非原子 persist、损坏静默清零）+ 本轮实测 1（export 越界）= 3 |

最终裁决：**通过**（全部必修项已修且复测闭环，拒绝项均有保守设计/转对端理由）。

### 6.4 C·验证（命令与实际输出）

```bash
$ python -m pytest -q
241 passed, 1 warning, 21 subtests passed in 6.13s          # exit 0（A3.1 后复跑）

$ node --test agent/tests/*.test.js
pass 228  fail 0  skipped 0                                 # exit 0

$ python scripts/audit.py
AUDIT: ALL CHECKS PASS                                      # exit 0（A3.1 后复跑）

$ python pipeline/build_opportunity_store_v1.py --db e2e_tmp/store.db
{"input_count": 95, "decision_count": 95, "buyer_count": 95, "integrity": "ok"}   # exit 0

$ python scripts/export_opportunities_for_agent.py --db e2e_tmp/store.db --out e2e_tmp/export.json
{"source_db": "e2e_tmp\\store.db", "row_count": 38, "ruleset_version": "opportunity-v1.1.0"}  # exit 0

$ python scripts/import_agent_outcomes.py --db e2e_tmp/store.db --in e2e_tmp/payload.json
{"deal_outcome_inserted": 1, "target_upserted": 1, "entity_links": 0, "promoted": 1}  # exit 0
$ python scripts/import_agent_outcomes.py --db e2e_tmp/store.db --in e2e_tmp/payload.json   # 重放
{"deal_outcome_inserted": 0, "target_upserted": 1, ...}    # exit 0，行级复核 1/1 不增（幂等）

$ .\run.ps1 -Down; .\run.ps1 -Up                            # 实机重启验收
stopped pid 28988 / 71216 / 65472（旧代码 agent 已停）
build 95/95 integrity ok → export 38 → import 0/0 → site 36136 / api 89916 / agent 78376

$ netstat -ano | grep 3317
TCP  127.0.0.1:3317  ...  LISTENING  78376                   # 仅回环（P0 #1 修复上线）

live matrix: site 4180 / →200；api 8000 /health →200；agent 3317 /api/health →{"ok":true,"provider":"deepseek-ready",...}
agent 静态矩阵：/ 200 · /index.html 200 · /db/* 403 · /server/* 403 · /..%2f..%2f 403 · /.env 403 · /quarantine/* 403 · 缺失 404
```

数据完整性：真实库 95 decisions / 95 buyers / deal_outcome 0 / discovered_target 0（e2e 全程未触碰真实库；-Up 重建后同值）。幂等性：同载荷双跑插入 0、行数 1/1 不变。

### 6.5 5.4 验收对照

| 验收项 | 状态 | 说明 |
|---|---|---|
| 四角色完整性 | ✓ | 首轮 4 隔离子代理真实盲审；复审轮内联 4 份报告（独立性打折已披露） |
| 盲审发现处置 | ✓ | B1 打回 5 必修 + B2 裁决表逐条闭环 |
| py_compile / 无循环导入 | ✓ | 全部改动文件编译通过；导入链路 e2e 实测 |
| 字段名统一 | ✓ | bridge 两侧字段契约测试全绿（16 项定向） |
| 每平台至少一条降级路径 | ✓ | audit.py 平台/降级检查全过（详见首轮报告） |
| 接口契约一致 | ✓ | contracts/opportunity-bridge-v1.md 双向测试 |
| 对抗攻击 ≥1 崩溃点 | ✓ | 3 处（2 P0 + 1 本轮实测），全部已修 |
| 安全自检 2.5 四条 | ✓ | F1 实机矩阵 + 静态 403 + 127.0.0.1 绑定 + /api/health |
| scripts/run_tests.py 全绿 | ✓（映射） | **仓库无 scripts/run_tests.py**；映射为实际测试入口：pytest 241+21、node 228/0 |
| 端到端 | ✓ | build→export→import→重放 全链实测（6.4） |
| 数据完整性 | ✓ | 95/95，0 残留（6.4） |
| 幂等性 | ✓ | 重放插入 0（6.4） |
| 第九章环境自检清单 | ✓（映射） | **仓库无第九章文档**；映射为 audit.py 环境自检（CORS/落地页/HTTP 200/实时接线） + 6.4 实机矩阵 |

### 6.6 最终结论

L2 全流程未跳过：A（2 轮 diff）→ cross_review（隔离盲审 + 内联复审）→ B（2 轮裁决，打回 1 次）→ C（全量验证）。修复 7 项缺陷 + 3 处崩溃点，全部复测闭环；测试全绿；实机重启验收通过；真实数据零损伤。审计通过，代码整体推送 main（见提交记录）。
